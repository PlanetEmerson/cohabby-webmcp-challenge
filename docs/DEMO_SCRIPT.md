# Final demo script V2

Target runtime: 2 minutes 45 seconds to 2 minutes 55 seconds. Record the real submitted build at 1920×1080 and 30 fps. CJ reads the narration in his normal voice. Use no music.

## 0:00 to 0:14

On screen: Start on the reset opening screen. Hold long enough to read the headline, CoHabby mark, six-step ribbon, and `Site tools ready` badge.

Narration:

> Finding a room is one thing. Finding someone you can actually live well with is harder. I built CoHabby Living to put roommate compatibility first.

## 0:14 to 0:31

On screen: Open the exact tool disclosure, then show the six-step ribbon. Add a small overlay listing the six exact tool names.

Narration:

> The page gives a browser agent six small WebMCP tools. They read the same state I see, five can change this page in real time, and each has one clear job. There is no chatbot behind it and no second agent.

## 0:31 to 0:50

On screen: Run `get_living_context`, then `stage_living_brief` with the New York example. Pause on **Does this feel like you?** and click **Yes, use these choices** yourself.

Narration:

> I start with a few everyday habits: budget, timing, pets, quiet time, and how I want shared space to feel. The agent can read these choices and stage a living plan, but only I can approve it.

## 0:50 to 1:10

On screen: Run `find_compatible_rooms`. Let Maya, Jordan, and Sam appear. Catch one shimmer pass on **Why Synergy?** Click anywhere on Maya and Jordan's cards to show whole-card selection.

Narration:

> Once I approve, the agent finds fictional people and homes that fit those needs. The person comes first. The room is useful context. I can select a whole card, or ask CoHabby why that roommate may fit.

## 1:10 to 1:32

On screen: Run `explain_synergy_match` for Maya. Hold on the large Synergy Lens, evidence ring, and three bounded reasons.

Narration:

> This Synergy Lens is the signature WebMCP moment. It opens from a live match and explains three practical reasons our household rhythms may work. This challenge uses demo scores.

## 1:32 to 1:54

On screen: Run `compare_shortlist` for Maya and Jordan. Show the solid comparison rows. Keep Maya selected and briefly move through the Warm, Direct, and Casual tone cards before returning to Warm.

Narration:

> Next, the agent compares two current roommates side by side. I can scan the Synergy read, rent, move timing, daily rhythm, and household boundaries. Then I choose who to greet and whether the note should sound warm, direct, or casual.

## 1:54 to 2:17

On screen: Run `prepare_introduction` for Maya with a warm tone. Show the larger portrait, editable hello, and human-only action. Click **Confirm demo introduction** yourself and hold on the exact receipt.

Narration:

> The final tool prepares a first hello using only facts already visible on the page. I can edit every word. The agent cannot send it or confirm it. That last click stays mine, and the demo says clearly that no real message was sent.

## 2:17 to 2:38

On screen: Use a clean three-shot proof sequence: one strict schema, one cancellation or render-acknowledgement test, then the public repository and live footer source revision. Keep each crop readable.

Narration:

> Under the hood, every tool has a strict schema, current-state checks, cancellation, safe output validation, and a render acknowledgement. The source, assets, and tests are public, and the live footer identifies the exact deployed revision.

## 2:38 to 2:53

On screen: Return to the success stage and let the two roommate paths meet at the home. End on the exact receipt and CoHabby mark.

Narration:

> CoHabby Living turns everyday habits into a thoughtful roommate introduction on one shared page. The browser handles the structured work. I keep the human decisions. That is the kind of web I want to build.

## Recording rules

- Record the real final deployment and real tool calls.
- Keep all six tool names readable at least once.
- Show both human-only actions as visible clicks.
- Keep the source revision visible near the end.
- Use straight cuts and restrained crops. Do not add generated footage, music, chat bubbles, romantic imagery, or claims that the demo calculates production Synergy.
- Keep the finished export below 2:55 so YouTube processing cannot push the visible runtime to three minutes.
